# Import the random module to generate random numbers.

# Define the main function where the program logic will be executed.
def main():
    # Start an infinite loop to continuously prompt the user for a level until valid input is received.
    while True:
        try:
            # Prompt the user for the level, which determines the range of the random number.
            # Convert the input to an integer.
            
            # Check if the input is 0. If so, do nothing (pass) and prompt again.
            # If the input is a valid number greater than 0, break the loop.
        except ValueError:
            # If a ValueError occurs (e.g., non-integer input), do nothing (pass) and prompt again.

    # Generate a random integer between 1 and n, inclusive, and assign it to x.

    # Start another infinite loop to handle the guessing part of the program.
    while True:
        try:
            # Prompt the user to guess the number and convert the input to an integer.
            
            # Check if the guessed number is greater than the target number.
                # If so, print a message indicating the guess is too large.
                
            # Check if the guessed number is less than the target number.
                # If so, print a message indicating the guess is too small.
                
            # Check if the guessed number is equal to the target number.
                # If so, print a congratulatory message and break the loop to end the game.
        except ValueError:
            # If a ValueError occurs during the guess (e.g., non-integer input), do nothing (pass) and prompt again.

    # Call the exit function to terminate the program.

# Execute the main function to run the program.
